import { createRoot } from 'react-dom/client';
import MultiWorkspace from '../app/multi-workspace';
import '../app/globals.css';

createRoot(document.getElementById('root')!).render(<MultiWorkspace />);
